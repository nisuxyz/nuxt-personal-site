services:
  - type: web
    name: itsnisuxyz
    runtime: node
    branch: trunk
    startCommand: bun --import ./.output/server/sentry.server.config.mjs .output/server/index.mjs
    buildCommand: bun install && bun run build
    envVars:
      - key: NITRO_PRESET
        value: render_com
